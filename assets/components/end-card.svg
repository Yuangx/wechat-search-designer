<svg xmlns="http://www.w3.org/2000/svg" width="{{width}}" height="{{height}}" viewBox="0 0 1080 {{viewHeight}}" role="img" aria-labelledby="design-title">
  <title id="design-title">{{title}}</title>
  <rect width="1080" height="{{viewHeight}}" fill="{{background}}"/>
  <rect x="48" y="48" width="8" height="44" rx="4" fill="{{accent}}"/>
  {{content}}
  {{band}}
</svg>
