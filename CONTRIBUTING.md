# 参与贡献

欢迎提出排版缺陷、兼容性问题、设计输入改进和文档修正。优先提供一个可复现的创作场景，说明实际结果和期望结果。

## 提交问题

附上 Skill 版本、操作系统、Node.js 与浏览器版本、使用的形态及输出状态。把文章、公众号名和图片替换成虚构材料，再提供最小输入。错误截图和日志应先移除姓名、路径、账号、密钥及客户资料。

涉及安全或隐私泄漏时按 [安全说明](SECURITY.md)处理。

## 修改代码

```bash
npm ci
npx playwright install chromium
npm run check
npm test
```

修改排版后，用三类示例核对 PNG、SVG 和手机图；修改输入或状态时覆盖相应的错误路径。测试记录逻辑的通过不代表视觉验收通过。

保持公众号名称准确、原文引用可核对、素材路径可移植，以及已有输出不被覆盖。只为已观察到的缺陷增加约束；文章理解和设计选择继续由 Agent 完成。

## 提交 PR

说明解决的问题、最终行为、实际验证和剩余限制。新增公开文件时同步维护 `distribution-files.json`；新增运行组件时更新 `assets/manifest.json` 中的文件哈希。运行 `npm run package` 检查实际 ZIP。

项目采用 [MIT 许可证](LICENSE)。请确保贡献内容可按同一许可证分发，并保留已有版权与许可声明；第三方内容须附来源与对应许可。真实文章、私人路径、账号导出、品牌原件和字体文件保存在本地忽略目录中。
