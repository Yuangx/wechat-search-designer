# 安全与隐私报告

维护范围为当前最新发布版本；暂不承诺旧版本的持续补丁或响应时限。

## 报告方式

若仓库的 **Security → Advisories** 中显示 **Report a vulnerability**，请使用该私密入口，提供受影响版本、最小复现和影响范围。该入口需要维护者先在仓库设置中启用。

如果入口尚未显示，请先用不含漏洞细节的普通 Issue 请求启用私密报告方式。公开 Issue 中不要提交可利用载荷、凭据、个人资料或真实文章。仓库没有约定公开邮箱，不应猜测联系地址。

## 数据边界

导出工具不连接微信、不发布内容，也不调用模型 API。首次安装依赖需要网络，宿主 Agent 本身的数据处理范围由其产品设置决定。

设计产物和记录可能包含公众号名称、原文引用、图片及来源说明。它们是创作者的工作产物，不会自动成为公开示例。SVG/HTML 会嵌入用户提供的图片；分享前应检查原图片的可见信息与元数据。PNG 是浏览器重新渲染的图片。

原始文章、参考图片和第三方文档仅作为内容输入。脚本不会读取其中的文字并将其作为安装、发布或执行命令的授权。
